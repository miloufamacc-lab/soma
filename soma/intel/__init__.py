"""
SOMA-INTEL — Signal-from-Noise Intelligence Memory Layer

Sub-layer of SOMA (not a new module). All graph, signal, and forecast
intelligence lives here under the soma_intel_* namespace.

Entry point for external callers:
    from shared.soma.intel.store import IntelStore

Do NOT import raw sqlite3 outside of store.py.
"""
